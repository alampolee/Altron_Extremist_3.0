python-dotenv
semantic-kernel>=1.6.2 # Or the latest stable version
openai>=1.12.0 # Required by semantic-kernel OpenAI connector
requests # For potential plugin implementations
# Add other specific dependencies if needed (e.g., AR/VR SDK bindings if interacting directly)